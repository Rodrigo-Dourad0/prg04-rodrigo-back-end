spring.application.name=RoteiroLivre
api.security.token.secret=${JWT_SECRET:123456}

# URL de Conexao (Porta 5432 para conexao direta)
spring.datasource.url=jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:5432/postgres

# Usuario
spring.datasource.username=postgres.gpubupouzursxipqijwm

# Senha
spring.datasource.password=${DB_PASSWORD:RoteiroLivre123}

spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


server.port=${PORT:8080}